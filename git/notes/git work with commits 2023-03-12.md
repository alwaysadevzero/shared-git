---
tags: 📥️/♐
aliases:
cssclass: 
---
- `Type:` [[&]]
- `Links:`
- `Reviewed Date:` [[2023-03-12]]
## Откатить файл до последнего коммита.
Вернуть файл в то состояние, в котором он был в последнем коммите. Например, если файл удален.
```bash
git checkout index.js (теперь, если прописать git status, то будет видно, что файл не изменен)
```

## Откатить файл до определенного коммита.
```bash
git checkout c73af1de0877fe25eb4648c2356742f253cc000d index.js

c73af1de0877fe25eb4648c2356742f253cc000d — хэш того коммита, до которого нужно восстановиться.
```

## Убрать файл из индекса (перед коммитом). 
Когда изменения подготовлены и уже добавлены в индекс (git add), но выяснилось, что какой-то файл индексировать не нужно.
```bash
git reset HEAD index.js (теперь, после проверки изменений git status, файл index.js не будет включен в индекс)
```

## Отменить последний коммит
Но команда revert оставляет историю, показывающую, что был и оригинальный и отмененный коммит. А иногда ненужный (или добавленный с ошибке) коммит нужно удалить из истории.
```bash
git revert HEAD (HEAD указывает на последний коммит, поэтому выполнили команду через него)
```

##  Исправить последний коммит
```bash
git commit --amend -m "Правильное сообщение коммита" (после этого последний коммит с неправильным текстом исчезнет из логов и хэш последнего коммита изменится — то есть не старый коммит поменялся, а создался новый коммит вместо старого)
```

## Убрать файл из последнего коммита и удалить этот файл.
```bash
git rm index.js
```
Осталось внести изменения в коммит, без внесения изменений в комментарий к нему:
```bash
Если прописать git status, будет следующее сообщение:

deleted: index.js

Поэтому можно изменить предыдущий коммит, убрав из него изменения:

git commit --amend --no-edit
```
## Убрать файл из последнего коммита, но не удалять этот файл.
```bash
git rm --сached index.js
```
Снова можно изменить прошлый коммит от внесения в него изменений:
```bash
Если прописать git status, будет такого плана информация:

deleted: index.js

Untrasked file: index.js (файл останется, но от теперь будет неотслеживаемым)

git commit --amend --no-edit
```